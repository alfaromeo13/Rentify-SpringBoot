package com.example.rentify.projections;

public interface ImageProjection {
    Double getSize(); //get sum for image sizes per apartment
    Integer getNumber(); //get number of images per apartment
}