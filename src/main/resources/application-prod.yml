spring:
  datasource:
    url: jdbc:mysql://PUBLIC_IP_ADDRS:PORT/rentify?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=CET&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8888